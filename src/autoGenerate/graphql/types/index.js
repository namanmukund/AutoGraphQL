import inputTypes from './inputTypes';
import schemaTypes from './schemaTypes';
import historySchemaTypes from './historyTypes';

const types = [...inputTypes, ...schemaTypes, ...historySchemaTypes];

export default types;

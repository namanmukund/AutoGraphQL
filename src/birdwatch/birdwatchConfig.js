// Event listeners for mutation hooks (Birdwatch Event Bus)
const birdWatch = [
  /*
  Example listener configuration:
  {
    on: ['createUser', 'createPost'],
    do: [
      {
        action: async (args) => {
          // Trigger async side-effect, notification, analytics, etc.
        },
        fields: {},
      },
    ],
  },
  */
];

export default birdWatch;
